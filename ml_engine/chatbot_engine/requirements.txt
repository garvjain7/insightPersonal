"""
requirements.txt for ml_engine/chatbot_engine/
==============================================

All dependencies needed by the chatbot engine.
Install with: pip install -r requirements.txt
"""

# Core data processing
pandas>=1.3.0
numpy>=1.20.0

# Database
duckdb>=0.5.0

# LLM / Embedding interaction
httpx>=0.23.0

# SQL parsing & safety
sqlglot>=10.0.0

# Async support (for Ollama HTTP calls)
# (httpx pulls in anyio, which is sufficient)

# Pydantic for validation (optional, but recommended)
pydantic>=2.0.0